<html lang="pt-BR">
  <%= render "home/head" %>
  <body>
    <!-- Conteudo -->
    <div class="container">
      <h2 class="text-center mb-4">Lista de Jogos</h2>
      <%= link_to new_game_path, class: "btn btn-sm btn-outline-primary" do %>
        <i class="bi bi-database-fill-add"> Adicionar um novo jogo</i>
      <% end %>
      <div class="table-responsive">
        <table class="table table-striped table-bordered text-center align-middle">
          <thead class="table-dark">
            <tr>
              <th>Nome</th>
              <th>Plataforma</th>
              <th>Gênero</th>
              <th>Status</th>
              <th>Dificuldade</th>
              <th>Avaliação</th>
              <th>Comentário</th>
              <th>Ações</th>
            </tr>
          </thead>
          <tbody>
            <% @games.each do |game| %>
              <tr>
                <td><%= game.name %></td>
                <td><%= game.platform %></td>
                <td><%= game.gender %></td>
                <td><%= game.status %></td>
                <td><%= game.difficulty %></td>
                <td><%= game.assessment %></td>
                <td><%= game.comment %></td>
                <td class="text-center align-middle">
                  <div class="d-inline-flex gap-2">
                    <%= link_to edit_game_path(game), class: "btn btn-sm btn-outline-primary" do %>
                      <i class="bi bi-pencil"></i>
                    <% end %>
                    <%= button_to game_path(game), method: :get, class: "btn btn-sm btn-outline-primary" do %>
                      <i class="bi bi-eye"></i>
                    <% end %>
                    <%= button_to game_path(game), method: :delete, data: { turbo_confirm: "Tem certeza?" }, class: "btn btn-sm btn-outline-danger" do %>
                      <i class="bi bi-trash"></i>
                    <% end %>
                  </div>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
        <!-- Paginação -->
        <%= paginate @games %>
      </div>
    </div>
  </body>
  <p style="color: green"><%= notice %></p>
</html>