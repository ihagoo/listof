<html lang="pt-BR">
  <%= render "home/head" %>
  <body>
    <!-- Conteudo -->
    <div class="container">
      <h2 class="text-center mb-4"><%= @game.name %></h2>
      <%= render @game %>
      <div class="d-inline-flex gap-2">
        <%= link_to games_path, class: "btn btn-sm btn-outline-primary" do %>
          <i class="bi bi-arrow-return-left"></i>
        <% end %>
        <%= link_to edit_game_path(@game), class: "btn btn-sm btn-outline-primary" do %>
          <i class="bi bi-pencil"></i>
        <% end %>
        <%= button_to game_path(@game), method: :delete, data: { turbo_confirm: "Tem certeza?" }, class: "btn btn-sm btn-outline-danger" do %>
          <i class="bi bi-trash"></i>
        <% end %>
      </div>
    </div>
  </body>
  <p style="color: green"><%= notice %></p>
</html>